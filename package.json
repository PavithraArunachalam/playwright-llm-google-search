{
  "name": "playwright-llm-google-search",
  "version": "1.0.0",
  "description": "LLM-driven Playwright + TypeScript automation framework generated from manual test cases",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0",
    "typescript": "^5.3.3"
  }
}
